/**
 * Public surface of the WebGL2 renderer.
 *
 * Everything under this folder is plain TypeScript with no Angular imports —
 * see README.md for why, and renderer.spec.ts for the test that enforces it.
 *
 * Filled in phase 1, after docs/RENDERER_SPEC.md is written.
 */

export const RENDERER_PLACEHOLDER = true;
